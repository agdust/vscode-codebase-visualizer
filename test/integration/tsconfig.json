{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"module": "CommonJS",
		"moduleResolution": "Node",
		"rootDir": "../..", // proj root so we can see both src and test
		"outDir": "../../dist/test"
	},
	"include": ["**/*.ts"]
}
